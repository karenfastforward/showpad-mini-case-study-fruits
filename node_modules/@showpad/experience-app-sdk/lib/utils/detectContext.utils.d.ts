import { AppContext } from '../types';
export declare const SHOWPADIZE_IFRAME_NAME = "showpadize";
/**
 * Get the context where your app is running.
 * Useful in combination with Showpadize for graceful degradation.
 *
 * @category Offline
 *
 * @example
 * ```typescript
 * import { Showpad } from '@showpad/experience-app-sdk'
 *
 * const appContext = Showpad.getAppContext()
 * ```
 */
export declare const getAppContext: () => AppContext;
export declare const isDev: () => boolean;
