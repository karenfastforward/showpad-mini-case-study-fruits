/**
 * Create Iframe with postmessages bridge to use methods from the Showpadize export
 *
 * @category Online
 *
 * @example
 * ```html
 * <!-- index.html -->
 * <!DOCTYPE html>
 * <html lang="en">
 *   <head>
 *     <meta charset="UTF-8" />
 *     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
 *
 *     <title>Showpadize App</title>
 *   </head>
 *   <body>
 *     <div id="app"></div>
 *     <script type="module" src="/src/main.ts"></script>
 *   </body>
 * </html>
 * ```
 * ```typescript
 * // main.ts
 * import { Showpad } from '@showpad/experience-app-sdk'
 *
 * const url = 'https://d69xwamum0h6z.cloudfront.net'
 * Showpad.showpadize('app', url)
 * ```
 */
export declare const showpadize: (domId: string, remoteUrl: string) => void;
